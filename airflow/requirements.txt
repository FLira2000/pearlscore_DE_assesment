# Extra packages on top of apache/airflow:2.10.5-python3.12
# Keep SQLAlchemy in the range Airflow 2.10 supports (pin in Dockerfile).
dlt[postgres,sql_database]>=1.4,<2
PyMySQL>=1.1,<2
# Airflow 2.10 ships SQLAlchemy 1.4; pandas 2.2+ expects SQLAlchemy 2.x.
pandas>=2.0,<2.2
dbt-core>=1.8,<1.10
dbt-postgres>=1.8,<1.10
